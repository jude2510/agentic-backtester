# Dependencies for Market Data MCP Server
# Used for both Lambda container and local migration scripts

# Core dependencies
boto3>=1.26.0
pyarrow>=10.0.0
pandas>=2.0.0
requests>=2.28.0
python-dotenv>=1.0.0

# PyIceberg for S3 Tables integration (minimal install). Pinned to the version
# market-data-pipeline writes with, so an image rebuild can never quietly pick
# up a different reader than the writer. Bump both together.
pyiceberg==0.11.1